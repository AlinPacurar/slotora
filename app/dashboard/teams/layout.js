// app/dashboard/teams/layout.js
// The sidebar shell is provided by app/dashboard/layout.js.
// This file just passes children through — required by Next.js App Router
// to prevent layout inheritance issues in nested route segments.

export default function TeamsLayout({ children }) {
    return children;
}